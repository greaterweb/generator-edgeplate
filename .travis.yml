language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-edgeplate' ]; then cd .. && eval "mv $currentfolder generator-edgeplate" && cd generator-edgeplate; fi
